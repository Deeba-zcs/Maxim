/** @type {import('next').NextConfig} */
const nextConfig = 
{
    images: {
        domains: ['bootstrapmade.com'],
      }
}

module.exports = {
    images: {
      domains: ['bootstrapmade.com'],
    },
  };